using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DroneAnimator : MonoBehaviour
{
    private Vector3 Rest = new Vector3(7.5f,0.7f,-7.0f);
    private Vector3 midway = new Vector3(8.75f, 0.7f,-6.0f);
    private Vector3 active = new Vector3(8.75f, 0.7f, -12.0f);

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
